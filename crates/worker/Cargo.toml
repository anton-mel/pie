[package]
name = "worker"
version = "0.1.0"
edition = "2024"

[features]
metal = ["models/metal"]

[dependencies]
anyhow = "1"
chat-template = { path = "../chat-template" }
candle-core = "0.11"
candle-nn = "0.11"
engine = { path = "../engine" }
hf-hub = "0.4"
models = { path = "../models" }
runtime = { path = "../runtime" }
serde_json = "1"
tokenizers = "0.23"

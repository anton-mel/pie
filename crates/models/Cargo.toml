[package]
name = "models"
version = "0.1.0"
edition = "2024"

[features]
metal = ["candle-core/metal", "candle-nn/metal"]

[dependencies]
anyhow = "1"
candle-core = "0.11"
candle-nn = "0.11"
serde = { version = "1", features = ["derive"] }
